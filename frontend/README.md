# ProperT — Frontend

Next.js App Router client for ProperT. Runbook and env: **repo root [`README.md`](../README.md)**.

```bash
npm install
npm run dev
```

Set **`NEXT_PUBLIC_API_URL`** (default `http://localhost:4000`). Chat Socket.IO uses **`src/lib/chatSocket.ts`** (JWT in `auth.token`).
